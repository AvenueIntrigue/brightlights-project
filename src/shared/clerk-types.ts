// shared/clerk-types.ts
export interface ClerkPublicMetadata {
  role?: string;          // 'Admin', 'User', etc.
  // Add any other metadata fields you use, e.g.:
  // plan?: string;
  // isPremium?: boolean;
}